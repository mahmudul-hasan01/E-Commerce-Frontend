import PageHeader from "../../Components/PageHeader/PageHeader";

const Blog = () => {
    return (
        <div>
            <PageHeader name={'Blog'} cutPage={'blog'} />
        </div>
    );
};

export default Blog;